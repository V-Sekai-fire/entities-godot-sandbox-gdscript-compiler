// doctest's main(), in a translation unit of its own so the implementation is
// compiled once for the whole suite instead of in every test binary.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
